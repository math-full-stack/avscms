# AVSCMS — Contexto do Projeto

## Stack
- PHP legado (sem namespace na maioria dos arquivos, sem composer, sem build).
- MySQL remoto (Cloud SQL) exposto em `127.0.0.1:3307` por `./tunnel.sh`; o MariaDB do XAMPP é residual. Entrypoints `.php` na raiz + `include/` (config, functions), `classes/`, `modules/`, `templates/` (Smarty-like), `language/`, `siteadmin/`, `sql/` (schema/migrations manuais).
- Ambiente local: `/Applications/XAMPP/xamppfiles/htdocs/avscms`, acesso via `http://localhost/avscms`.

## Convenções
- Não introduzir build step, framework novo ou refactor amplo sem pedido explícito.
- Manter estilo do entorno (naming, includes via `require/include`, SQL inline).
- OWASP: escapar output, prepared statements onde houver wrapper; nunca concatenar `$_GET/$_POST` em SQL/shell.

## Risco neste repo (sempre classificar antes de agir)
- Low: edição local em template/language, `php -l`, leitura.
- Medium: mudança em `include/`, `classes/`, `modules/`, entrypoints compartilhados — exige teste do fluxo afetado + adjacente.
- High: `sql/` destrutivo, `rm`, `siteadmin/` com efeito produção, push/deploy — exige confirmação explícita do usuário.

## Verificação padrão (sem suite automatizada)
1. `php -l <arquivo>` no arquivo alterado.
2. Quick Smoke: smoke manual do fluxo via XAMPP.
3. Targeted se multi-arquivo: fluxo alterado + 1 adjacente + 1 caso de erro.
4. Declarar gap: "sem testes automatizados, verificação limitada a lint + smoke".

## Memória viva (atualizar ao descobrir)
- Goal atual: ativação automática do .opencode via opencode.json.
- Decisão: `opencode.json: instructions` carrega AGENTS.md + projeto/ia/skills/sistema/coordenador; `permission.skill * : allow`.
- MD3 no tema: header.tpl/enter.tpl carregam Roboto + Material Symbols Rounded (Google Fonts) e css/pornozinho-md3.css (tokens --md-sys-*); js/md3-ripple.js roda em todas as páginas (seletor RIPPLE_SELECTOR inclui .avs-settings-tab/.avs-settings-item). embed.tpl não carregava MD3 — fontes adicionadas em seu head.
- Vidro (frosted glass macOS): tokens `--xb-glass-*` (bg opaque translúcido, border rgba branco, hl inset superior, blur sm/full) definidos em `pornozinho.css` e republicados no `:root` do `pornozinho-md3.css`. Aplicado em: topbar ao rolar (`.xb-topbar-scrolled`, MD3 antes anulava com `backdrop-filter:none`), mega menu `.xb-dropdown-menu`, dropdowns comuns, `.modal-content`, `.xb-mobnav`, autocomplete, menus do player mediabunny (`avs-dl-menu` e `.avs-settings`, agora `blur(20px) saturate(180%)` sobre o vídeo). Regra de ouro: blur só aparece se houver pixels atrás (fundo chapado não desfoca). Se mexer em avs-player.css subir `?ver` IGUAL em `video_mbplayer.tpl` e no bloco do mini em `footer.tpl` (hoje 3.2.6); md3.css e pornozinho.css têm `?ver` próprios no header.tpl (md3 1.0.4, pornozinho 1.0.7, e enter.tpl usa o mesmo ver do md3). Disclaimer 18+ (`xb-agegate` + `enter.tpl`) permanece intocado — é o padrão de referência.
- Player mediabunny usa MD3 no menu de configurações (abas Qualidade/Velocidade, Material Symbols para ícones/check, tokens --md-sys-* com fallback). Se novos componentes do player usarem Material Symbols, manter `embed.tpl` com as fontes.
- Cor viva / modernidade (12/09): tokens DRY `--xb-accent-2` (violeta #7c4dff/#9a6bff), `--xb-grad`, `--xb-grad-v`, `--xb-glow`, `--xb-glow-soft`, `--xb-glow-v` no `:root` de ambos os CSS. Aplicados em: body (aurora com 3 radiais: magenta+violeta+ciano), scrollbar thumb gradiente, `.btn-primary` e `.xb-action-upload` (gradiente + glow + background-position animado), `.xb-section h2`/`h2 i` e `.xb-hero-title` (texto/ícone em gradiente com `background-clip:text`), underline do nav ativo, círculo de play do thumb, hover dos thumbs/tags/cards (`.xb-creator-card`, `.xb-cat-card`), chips, tabs, paginação, `.progress-bar`, `.xb-mobnav` ativo e `::selection`. Ícones: hover preenche Material Symbols (`FILL 1`) com glow nos itens de menu; actions FA (`.xb-actions .xb-action i`) ganham cor accent + `text-shadow` glow no hover. Disclaimer `xb-agegate`/`enter.tpl` permanece intocado (só o `?ver` do md3 subiu). Versões: pornozinho.css 1.0.8, md3 1.0.5.
- Dark/Light (12/09): tema claro via `html[data-theme="light"]` no `pornozinho-md3.css` (paleta M3 light seed #FF1493 + remap `--xb-*` + aurora/scrollbar claras + fix de textos legados + topbar-scrolled e mobnav com vidro claro). Script inline no `<head>` do header.tpl aplica o atributo ANTES da 1ª pintura (anti-FOUC): `localStorage.avs_theme` ou `prefers-color-scheme` na 1ª visita (dark segue sendo o default). Botão `[data-theme-toggle]` no `.xb-actions` (Material Symbol muda light_mode/dark_mode). **Decisão:** janelas de vidro (mega menu, dropdowns, autocomplete, modais) e o player permanecem ESCUROS nos dois temas — textos desses menus foram "pinados" de volta ao dark no bloco light (`.dropdown-item`, `.modal-title`, `.xb-dropdown-*`, `.xb-mm-item.xb-mm-current`, autocomplete). `enter.tpl` não carrega o script → sempre dark. md3 agora 1.0.6 (header.tpl + enter.tpl). Caveat: view.php/embed e a cauda de ícones cinza hardcoded do video.tpl não foram auditadas no light — conferir visualmente.
- Logo viva (13/09): `images/logo/logo.png` foi RECOLORIDA para o gradiente **roxo com rosa** (#7C4DFF → #FF1493) e RECORTADA para o bounding box da arte (2172x724 → **1749x578**, pad 6px; depois **1749x354**, cortando o bottom: wordmark vai de y~40..379 e havia ~180px de vazio + uma tira decorativa/tagline em y 560..577 — removida), removendo as margens transparentes. No HEADER a logo é um **gradiente animado em onda** (tons roxo↔rosa) recortado pelo formato da arte via MÁSCARA: `.xb-logo` recebe `mask-image:var(--xb-logo-img)` (URL injetada inline no header.tpl:306) sobre um `background` linear animado (`@keyframes xb-logo-wave`, 6s alternate); a `<img>` fica `visibility:hidden` (só reserva layout). Fica viva nos DOIS temas (não usa `html[data-theme]`). Tamanhos: desktop `max-height:150px`, mobile `46px`. `enter.tpl:44` (age gate) e `index_logo.tpl` seguem mostrando o PNG estático recolorido (mesmo arquivo); upload no siteadmin substitui o arquivo (e muda a máscara junto). Recolor+recorte via GD (alpha preservado; sem pixels brancos). Tamanhos: desktop `max-height:60px`, mobile `36px` (usuário insistiu "a logo está muito grande" após recorte + bottom; 100/40, 150/46 e 110/46 anteriores). Ajustar ver a cada mudança. PENDENTE: usuário quer remover o 2º "P" do wordmark ("P Pornozinho") mantendo o 1º "P" com ícone de play — análise de layout iniciada (wordmark conectado x368..1742, original tinha pixels coloridos em x[427..1739] y[206..485]); precisa localizar a faixa x do 2º P no artefato. Versões: pornozinho.css=1.0.10 (header.tpl:99), md3=1.0.9 (header.tpl:107 + enter.tpl:33).
- Mega menu E player SEGUEM o tema (reversão parcial da decisão escuro-dentro-dos-dois): no light o `.xb-dropdown-menu` vira vidro claro no desktop (`@media min-width:992px` no bloco light do md3; mobile já flipava via `surface-container-high`) e os pins de texto dark do submenu foram removidos/trocados por tokens. `avs-player.css` ganhou vars `--avs-menu-{bg,border,hl}` (dark default em `.avs-player`, flipadas no light block) substituindo o rgba scuro hardcoded de `.avs-settings`/`.avs-dl-menu`. PERMANECEM escuros nos dois temas: dropdowns comuns Bootstrap, autocomplete, modais. Versões: avs-player.css=3.2.7 (pair video_mbplayer.tpl:172 + footer.tpl:469 — subir juntos), md3=1.0.8 (header.tpl:107 + enter.tpl:33). Caveat: barra de controles do player usa `surface-container-highest` → fica clara no light (conferir visual).
- Mini player no site (substitui o PiP nativo, removido): botão `data-action="mini"` + `.avs-mini-actions` (expandir/fechar) em `video_mbplayer.tpl`; classe `.avs-mini-mode` fixa o player no canto ao rolar (IntersectionObserver, "enquanto toca" + preferência `avs_mini_auto`, localStorage default on); persistência `sessionStorage.avs_mini_state` {vid,source,time,href,ts} gravada ~5s + pagehide/visibilitychange, limpa em pause/end/X; resume na página do vídeo via `seekToTime(time,true)` no boot. `avs-mini.js`+`avs-mini.css` (no `footer.tpl`, hoje 2.1.0) hidratam a janelinha das demais páginas (`<video>` nativo) retomando do tempo salvo — o HTML dela vem do template, ver "Mini é HTML DE TEMPLATE" abaixo.
- ARMADILHA do mini: NUNCA observar o próprio player para decidir SAIR do mini. Ao entrar em `.avs-mini-mode` ele vira `position: fixed` no canto, volta a intersectar o viewport e o observer manda sair — laço medido em 141 ativações/141 desativações em 8s (o player "piscando"). Entrar: observar o player enquanto ele está no fluxo. Sair: observar `.avs-mini-spacer`, div que reserva o lugar do player (não se move por nossa causa e de quebra evita o salto de layout). Para testar, contar transições de classe com MutationObserver — a olho nu não dá para ver que é laço.
- Mini das outras páginas = MESMO UI do mini da página do vídeo: as classes são `avs-player avs-mini-mode avs-fallback` (canvas → `<video class="avs-fallback-video">`, o caminho nativo que o próprio player já usa) e a barra de controles, o menu (abas Qualidade/Velocidade/Reprodução) e o sprite são os mesmos de `video_mbplayer.tpl`. O estado `avs_mini_state` leva `sources` + `poster` para o menu ter as qualidades e a janela ter capa. `avs-mini.css` ficou só com a nota de que o visual mora em `avs-player.css`.
- Mini é HTML DE TEMPLATE, não DOM de JS (mudança 12/09): `footer.tpl` renderiza `#avs-mini-floating` + o `<link>` de `avs-player.css` quando existe o cookie `avs_mini` (gravado por `avs-player.js` em `miniPersist`, limpo em `miniClear`, max-age 180s, só se `miniPref`). O `avs-mini.js` só HIDRATA: acha o shell, valida o estado e liga os controles. Antes o JS montava ~45 nós, injetava o CSS DEPOIS do parse (o browser só descobria no fim) e pintava a janela sem estilo por um instante — era o "parece que recarrega a cada página" que o usuário relatava. O shell sai com `style="display:none"` inline e só aparece no fim da hidratação; sem estado válido (`ts` velho, aba nova, pref desligada) o JS REMOVE o shell e limpa o cookie, então nunca sobra quadrado vazio. `window.__avsPlayerCss` não existe mais. É por isso que `window.__avsReady`/`__avsModuleLoaded` (só da página do vídeo) não têm nada a ver com este mini.
- Armadilha do `$smarty.cookies` no template: usar `{if isset($smarty.cookies.avs_mini) && $smarty.cookies.avs_mini == '1' && !$view}`. Sem o `isset`, o Smarty 3.1 gera `Undefined array key "avs_mini"` no log em TODA página sem o cookie. O `!$view` existe porque a página do vídeo tem o mini interno; `view.php`/`embed.tpl` nem renderizam o footer. O `avs-mini.js` também remove o shell em qualquer página com player, como rede de segurança.
- Versão dos assets do player: `video_mbplayer.tpl` (link+script) e o `<link>` dentro do bloco do mini no `footer.tpl` precisam do MESMO `?ver` (hoje 3.2.5) — subir junto ao mexer em `avs-player.{js,css}`. O `avs-mini` tem o seu (2.1.0, junto com `avs-mini.css`). Sem isso a página do vídeo pega o arquivo novo e o mini pega o velho do cache.
- Trade-off do cookie `avs_mini`: vai em toda requisição (é só a flag `1`) e dura até 180s. Se o site estiver atrás de cache que não atende requisição com cookie (Varnish default), o HTML dessas visitas deixa de ser cacheável nesse intervalo. Aceitável: é flag, não dado, e expira rápido.
- Mini no celular: o botão de pip da janelinha leva de volta para a página do vídeo (não existe "player grande" na página atual) e o menu de configurações rola dentro da janela (`max-height: calc(100% - 82px)`), senão era cortado pelo `overflow: hidden`.
- Aberto: mapear fluxos críticos (upload, auth, player) e arquivos de config reais em `include/`.
- Banco: NÃO é o MariaDB do XAMPP (`var/mysql` local é residual e `mysql.server start` falha com Errcode 13). O site usa o Cloud SQL remoto, exposto em `127.0.0.1:3307` por `./tunnel.sh` (`status`/`stop` também); `include/config.db.php` lê credenciais do `.env` (DB_HOST=127.0.0.1, DB_PORT=3307, DB_NAME=avs). Leitura local: `set -a; . ./.env; set +a` + `/Applications/XAMPP/xamppfiles/bin/php` (o `php` do CLI não está no PATH).
- Armadilha de include: `include/function_global.php` declara 36 funções incondicionais no topo, o que dispara early binding — guard `if (defined(...)) { return; }` NÃO evita o redeclare. O guard precisa envolver as declarações (`if ( !defined('AVS_FUNCTION_GLOBAL_LOADED') ) { ... }`). Sem isso, o `require_once` de `config.php` (rotação de capas) + o `require` dos entrypoints fataliza com "Cannot redeclare get_request()" e derruba TODAS as páginas em 500.
- Mediabunny empacotado local: `media/player/mediabunny/mediabunny.min.js` (v1.55.6, de `cdn.jsdelivr.net/npm/mediabunny@1.55.6/dist/bundles/mediabunny.min.mjs`), importado por `avs-player.js` via `./mediabunny.min.js?ver=<versão da lib>`. NÃO voltar para CDN: import estático que falha impede o módulo INTEIRO de executar (tela ficava só com "desative bloqueadores"; reproduzido com `Network.setBlockedURLs` no probe). Manter extensão `.js`, não `.mjs`: o Apache do XAMPP não tem MIME para `.mjs` (serve sem `Content-Type` e o browser recusa módulo); `.js` sai como `application/x-javascript`, que é válido.
- Cache-buster do player: `avs-player.{css,js}?ver=N` em `video_mbplayer.tpl` (hoje 3.2.2) precisa subir a cada edição dos arquivos — um teste com perfil de navegador reaproveitado mediu o layout ANTIGO e só acusou o fix depois de limpar o cache.
- Fallback do player no `video_mbplayer.tpl`: `avs-player.js` marca `window.__avsModuleLoaded` ao subir; o template acusa bloqueador só se o módulo NÃO rodou em ~8s, e aviso de lentidão só em ~45s, parando quando `__avsReady` vira true. Antes era um `setTimeout` de 10s cego que mentia sobre bloqueador em carga lenta (e seguia rodando depois do player pronto).
- Menu de configurações do player: as 3 abas (ícone + rótulo) pedem ~336px. Painel de 280px + `flex: 1 1 0` (não encolhe abaixo do `min-content`) + faixa `overflow-x: visible` faziam a aba "Reprodução" sair ~75px para fora do painel. Estado atual: painel `min(360px, calc(100% - 24px))`, faixa com `overflow-x: auto` e aba `flex: 1 0 auto`. Ao mexer, MEDIR (`scrollWidth` vs `clientWidth`, e a borda direita de cada aba vs a borda interna do painel) — não confiar em inspeção visual.
- Viewport do tema (pré-existente, `header.tpl`): `content="width=1280, initial-scale=1, maximum-scale=1, user-scalable=no"`. No celular o layout é 1280px escalado, então as `@media (max-width: 767px/575px)` do player NUNCA casam no mobile — regra de responsividade tem que caber no layout de 1280, não em media query de viewport.
- Estado do player 12/09: `__avsReady` vira true em ~1,7s local; com o bundle local o player carrega inclusive com jsdelivr bloqueado (módulo/ready true, sem mensagem).
- Smoke do mini no template 12/09: com cookie, a home traz o shell JÁ no HTML (nó adicionado em ~1,4s, visível em ~2,3s e já com `position: fixed` 400/16/88 — sem piscada sem estilo); sem cookie, nada renderizado e `avs-player.css` nem é pedido; `/video/156/<slug>` e `/view.php?VID=156` sem shell; cookie sem estado e estado com `ts` velho removem o shell e limpam o cookie; vídeo retoma tocando do tempo salvo; 0 exceções no console. Medir isso sem olhar: espionar `MutationObserver` para pegar o `style` inline no instante em que o nó entra no DOM — é a assinatura de que veio do servidor.
- Smoke 12/09 (túnel 3307 ativo): home 200, `/video/156/<slug>` 200 com avs-player.js/css + avs-mini.js/css (200), videos/categories/login/signup 200, `/pagina-inexistente` 404, sem fatals novos no `php_error_log`. Warnings pré-existentes (search_type, ad, autoplay em video_mbplayer.tpl) não vêm do diff. Verificação segue sem suite automatizada (lint + smoke).
- Modo camuflado (13/09): botão `.xb-action-disguise` (ícone `visibility_off`, ao lado do toggle de tema em `header.tpl`) + atalho Y+Y (~800ms, ignorado em inputs) ativam um overlay fullscreen que disfarça o site como "AutoVitrine", uma vitrine de vídeos de carros. `templates/frontend/pornozinho/js/avs-disguise.js` (carregado no `footer.tpl`, `?ver=1.0.0`) monta o overlay do ZERO a cada ativação (iframes recomeçam limpos e ao sair o remove → nada continua tocando), troca `<title>`/favicon por identidade automotiva, pausa todo `video/audio` da página (retoma os que tocavam ao sair), trava scroll e tem busca que filtra os cards. CSS em `css/avs-disguise.css?ver=1.0.2` (linkado no `header.tpl`) com identidade próPRIA azul aço — de propósito não usa rosa/gradiente do site para não parecer o mesmo domínio. Overlay usa o viewport REAL (position:fixed), então as `@media` dele funcionam mesmo com o layout 1280px do tema (diferente do resto). O botão da topbar (`::after` + `attr(data-tip)` em avs-disguise.css) tem tooltip próprio explicando o atalho Y+Y, ícone menor (18px) e `outline:none` no foco (o `:focus-visible` global do md3 dava anel rosa ao clicar). Vídeos: 5 IDs reais de carros verificados via oembed do YouTube (não-empacotados) listados em `VID_LIST` no topo do JS — única parte a editar para trocar a vitrine. Versões dos assets próprias (JS 1.0.0, CSS 1.0.1); não conflita com avs-player/mini.
- Botão repetir = 3 estágios (14/09): o ícone `[data-action="repeat"]` na barra do player agora cicla desligado → repetir vídeo atual → próximo automático. `syncRepeatUi()` deriva o estágio das DUAS preferências (`avs_repeat` e `autoplayNext`, que seguem independentes no menu de configurações): `repeat` (loop) tem precedência sobre `next`. Estágio `next` troca o glifo do sprite para `#avs-i-next` (skip-next), adicionado em video_mbplayer.tpl, e ganha `.avs-rpt-next` (ícone escalado). Clique: se estiver em repeat → desliga repeat + liga autoplayNext; se em next → desliga; se off → liga repeat. **"Próximo vídeo" agora é DESLIGADO por padrão** (só `'1'`/legado `'true'` ativa — `video.tpl` check e `showAutoplayNext` aceitam os dois; grava `'1'/'0'`). A tela final continua aparecendo SEMPRE; o estágio `next` é o único que roda o countdown automático. **CANCELAR (e clique no fundo) não fecha mais a tela final** — `cancelAutoNav()` só congela o countdown (some o círculo); o usuário segue navegando por PRÓXIMO ou pelos cards. Versões: JS 3.2.13→3.2.15 (3.2.14 tooltips de vídeo_mbplayer.tpl/settings; 3.2.15 = CANCELAR só congela o countdown) + CSS 3.2.9→3.2.10 anteriores (pair video_mbplayer.tpl:178-179 + footer.tpl:469).
- Busca pausada (±10 centrais, setas ±5) (13/09): o `flashCenter()` de 600ms escondia o overlay central MESMO pausado — o ícone sumia e o próximo clique no mesmo lugar togglava play em vez de buscar (parecia "buscas pausadas bugadas"). Fix: `seek()` do teclado e botões centrais (`seekCenterBy`) capturam `wasPlaying` antes de buscar e decidem o overlay — pausado usa `showCenter()` (permanece, permite busca contínua), tocando usa `flashCenter()`. O mini (avs-mini.js, `jump`) já fazia certo (center permanece pausado via syncPlayIcon). JS ver: 3.2.7→3.2.8 (video_mbplayer.tpl:173).
- Overlay "próximo vídeo" redesenhado + bug do countdown (13/09): o `render()` chamava `onEnded()` a CADA rAF depois do fim (playbackTimeAtStart fica fixo em endTimestamp) → `showAutoplayNext()` reiniciava o countdown 3 a cada frame (nunca navegava) e ressuscitava o CANCELAR na hora. Fix: guard `else if (!endedFired)` no branch de fim + reset de `endedFired=false` e `hideAutoplayOverlay()` em `play()` (quando reinicia do fim), `seekToTime()` (alvo < end) e no branch de loop do render. Novo overlay `#avs-autoplay` (substitui `#autoplay-overlay`): layout 2 colunas dentro do player — ESQUERDA "Mais vídeos" com até 4 cards padrão do site (`.thumb-overlay.xb-portrait` + `.xb-trio` de 3 capas + `.duration` + chip `.xb-thumb-meta` com views/`@user`/título em marquee no hover, sem título abaixo — igual ao grid da home; hover roda capas restantes via `.avs-autoplay-pv` com `z-index:3` como o `<video>` de preview da home, réplica JS de `video_cover_list/trio` de function_global.php), DIREITA "Próximo vídeo" (countdown de 3s + capa + título + views/rate + CANCELAR/PRÓXIMO). **Tema**: o overlay segue light/dark — fundo via var `--avs-autoplay-bg` (escuro default em avs-player.css, flip claro no bloco light do pornozinho-md3.css, padrão `--avs-menu-*`), textos/bordas via tokens `--md-sys-color-*`. Dados: `related_videos_data` (player_settings.tpl) ganhou `thumbs`,`opt`(thumbnails_opt),`orient`(orientation),`user`(username),`views_w` por vídeo; base das capas vem de `thumb` strip `/NNN.jpg`. Clique no fundo do overlay = CANCELAR. `video.tpl` NÃO foi mexido (CSS antigo `.autoplay-*` fica dormente; novo CSS mora em avs-player.css escopado `.avs-autoplay`). Religar: cliques quando overlay visível excluem `#avs-autoplay` (não toggla play). VERSÕES: JS 3.2.10→3.2.12 + CSS 3.2.8→3.2.9 (pair video_mbplayer.tpl:178-179 + footer.tpl:469) + md3 1.0.9→1.0.10 (header.tpl:107 + enter.tpl:33, subido no redesign de tema/cards). Caveat: com "Repetir vídeo" ON o overlay não aparece (loop silencioso, `onEnded` nunca dispara). **"Próximo vídeo" OFF não esconde mais a tela final** (pedido do usuário): o toggle agora só desliga a reprodução AUTOMÁTICA — a tela final sempre aparece no fim, sem countdown (`.avs-autoplay-count` oculto), com o botão PRÓXIMO/click no card para navegar manualmente.
- Home responsiva real (13/09): a home agora renderiza com viewport `width=device-width` — meta condicional no header.tpl:28 (`{if isset($index) && $index}`... else 1280); `$index` é setado SÓ em index.php:150, e `$menu == 'home'` não serve (login/signup/notices/feedback/notfound/static/mail/requests também setam 'home'). Uma regra que "acorda" o CSS mobile dormente (mobnav, dropdown M3 bottom-sheet, grids de covers/carousel, `body{padding-bottom:58px}`). Novo bloco `RESPONSIVO HOME` em pornozinho.css (antes do AGE GATE): `@media 991.98` (`.xb-section` compacto 17px; **`.content-row .content-title{display:-webkit-box!important}`** — no touch não há hover/marquee, então o título volta abaixo do thumb nos TRÊS grids (Novos/Em alta e hero lateral) com uma só regra via `.content-row` compartilhado; hero lateral 11.5px/max-height 31) e `@media 575.98` (`.container` padding 16px; grid padding-bottom 10; hero title/meta/overlay calibrados; `.xb-creator-card` e `.xb-cat-card` compactos; tags-lg 12px; carousel temático `margin:1.5rem -1rem` `padding:1.5rem .75rem` `border-radius:20px`). **DRY dos grids**: index.tpl passou a usar `{include file='video_card.tpl' v=$recent_videos[i]}` / `v=$viewed_videos[i]` — partial novo `templates/frontend/pornozinho/video_card.tpl` (fonte única: thumb/video_trio, label-private, xb-thumb-meta, duration/HD, content-title, xb-tags max=4; markup validado IDÊNTICO ao renderizado antes, diff só whitespace). Home perdeu `d-none d-md-block` no hero lateral → `xb-hero-side-col` (cards pequenos aparecem no mobile, `grid` 2-col). `.container` ganhou cap `max-width:1280px;margin-inline:auto` (só afeta monitores largos). Desktop comics: hero lateral segue sem título abaixo da thumb em ≥992 (`.content-row .content-title` nô continua no desktop — look aceito). Nas outras páginas viewport permanece 1280 e o bloco fica dormente (mesmo escalado de sempre). Versões: pornozinho.css→1.0.12 (header.tpl:99). Smoke 13/09: home 200 com meta device-width + 48 cards + hero lateral; `/videos` 200 com meta 1280; categories/users 200; markup do card antes/depois = mesmo; sem fatals novos no php_error_log (warnings pré-existentes de search_type seguem e não vêm do diff). Gap: sem suite automatizada — visual em viewport real (DevTools mobile) NÃO conferido, só curl/lint estrutural.
- Header home mobile (13/09, continuação): no device-width o topbar estourava — logo (máscara ~4.9:1, 36px→178px) + 5 ações ícone (md3 padding 8px16 deixa theme 54px) + hamburger ≈ 490px mínimos numa tela de ~343 úteis. Fix no bloco RESPONSIVO HOME do pornozinho.css: `.xb-topbar-inner{justify-content:space-between}` (logo ESQUERDA, grupo ações+hamburger ancora na DIREITA), compactação ≤575 (logo max-height 20px→~100px, actions padding 7px9/gap 2, ícones material 20px, upload 7px11, hamburger 5px8; spec `.xb-topbar-inner .xb-actions .xb-action` vence md3 8px16 e avs-disguise) e tier ≤389 (logo 18px, padding 6px7, ícones 18px) que ainda cabe em 320. Verificação via browser headless same-origin (probe temporário dentro de templates/, vista depois quem é o único diretório servido estático) — 485=485 sem overflow no doc, logo 16-116, act 244-469, hamb 437-469 (na borda). Não confiar em medição via `file://` (origem cruzada desaplica CSS Parcialmente) nem em `--window-size` do headless novo (<500 vira 500). md3 light ganhou `.xb-search|xb-mobilesearch .xb-search-input` com cor `--xb-text` (base era #fff, invisível). Versões: pornozinho.css→1.0.15 (header.tpl:99), md3→1.0.13 (header.tpl:107 + enter.tpl:33). Append: não confiar em medição via file://.
- Videos responsivo (13/09): `/videos` (e `/videos/<slug>`) agora renderiza com viewport `width=device-width` via flag `$smarty->assign('video_listing', true)` em videos.php (antes do display); header.tpl:28 aceita `{if (isset($index)&&$index) || (isset($video_listing)&&$video_listing)}`. Grid = DRY total: videos.tpl usa `{include file='video_card.tpl'}` com `{capture name=videos_cols}` reproduzindo AS MESMAS classes atuais (`{if $min_col=='2'}col-6 {/if}col-sm-6 col-md-4 col-lg-4{if $max_col=='5'} col-xl-3{/if}`) e `show_tags=0` (listado nunca mostrou tags; palavras-chave até são normalizadas em videos.php:143-145). video_card.tpl ganhou `card_cols` (default `col-6 col-sm-6 col-md-4 col-lg-3` da home) e `show_tags` (default 1). CSS: `content-left/.content-right` já empilham <992 (style.css:546); bloco novo "Videos — responsivo real" (pornozinho.css, antes do AGE GATE): ≤991.98 aperta well-filters/btn-group/dropdown (barra completa no tablet não estoura), ≤767.98 `.well-filters{display:flex;flex-wrap:wrap;gap:4px 14px}` + `.float-right{margin-left:auto}` (floats matavam o header mobile), ≤575.98 chips 999px p/ dropdown "Filters" e Upload (`.btn-group .well-action.dropdown-toggle`, `.well-filters > .float-right a`, `box-shadow:none`), well-info 12px, sidebar vira grid 2-col de chips (`.content-right .list-group{display:grid;cols repeat(2,1fr);gap:8px}`; item 999px/13px/ellipsis). Grid/títulos do listado herdam o RESPONSIVO HOME (.content-row). Verificado: probe same-origin doc 500=500 sem overflow, cl/cr 498 cada empilhados; /videos e /videos/teen 200 com meta device-width; 32 cards p/ página (16 na pág 2); home segue com 48 cards e sem xb-tags; /search mantém 1280; sem fatals novos. pornozinho.css já estava em 1.0.16 (header.tpl:99) quando editei.
- Ícones no menu + fim do badge NOVO (14/09): o badge "NOVO" do Shorts foi REMOVIDO do site inteiro (header.tpl:392 e index.tpl:25) junto com as regras mortas `.xb-badge-new` e `.xb-shorts-nav-icon` de `avs-shorts.css`. Armadilha descoberta: `avs-shorts.css` só é linkado `{if isset($shorts_page) && $shorts_page}` (header.tpl:110), então no resto do site aquele badge e o ícone do Shorts apareciam SEM estilo (texto "NOVO" cru ao lado de "Shorts") — bug visual mascarado; por isso qualquer estilo novo de item de menu tem que nascer em `pornozinho.css`/`md3`, nunca em avs-shorts.css. O ícone `play_circle` do Shorts virou o PADRÃO: todos os itens do nav do header ganharam `<span class="material-symbols-rounded xb-nav-icon">` (Início=home, Em alta=trending_up, Novos=schedule, Shorts=play_circle, Vídeos=videocam, Categorias=category, Creators=group, Tags=sell, Fotos=photo_library, Blogs=article, Comunidade=forum) e a `.xb-mobnav` (footer.tpl) trocou Font Awesome por Material Symbols com `xb-mobnav-icon` (Início=home, Shorts=play_circle, Hot=local_fire_department, Upload=add_circle, Categorias=category, Perfil=person). `.xb-nav-icon` mora em pornozinho.css (sempre carregado); no md3 hover/ativo preenchem (`FILL 1`) com glow rosa e o item ativo do mobnav também. Os seletores `.xb-mobnav a i` ficaram nos dois CSS (o mobnav não usa mais `<i>`, mas nada quebra). Versões: pornozinho.css 1.0.21 (header.tpl:99), md3 1.0.18 (header.tpl:107 + enter.tpl:33), avs-shorts.css 1.0.1 (header.tpl:110). Smoke 14/09: home/shorts 200, 9 ícones de nav renderizados (Fotos/Blogs/Comunidade só se o módulo ligar), 6 no mobnav, 0 ocorrências de badge/NOVO, CSS servido 200, braces balanceados, sem fatals novos.
- Home: Shorts no TOPO (14/09): a seção de Shorts (`{if $shorts_videos}` + shelf) subiu para a PRIMEIRA posição do `index.tpl`; a ordem dos blocos ficou Shorts & Reels → shelf → Novos vídeos → Destaques. Só reordenação de markup (nenhum CSS/query novo); o bloco de "Novos vídeos" foi movido para antes de `{if $hero_videos}`. Verificado no HTML servido e no headless.
- Logo maior no mobile (14/09): `.xb-logo img` no bloco RESPONSIVO HOME do `pornozinho.css` subiu `≤991.98` 23→**30px**, `≤575.98` 17→**26px**, `≤389.98` 14→**22px** (base desktop segue 41px em `.xb-logo img`). Medição com CDP `Emulation.setDeviceMetricsOverride` — NÃO usar `--window-size` do headless novo (não desce de 500). Resultado: `overflowX=false` em 320/360/390/414/480/575/768; em 320 a logo fica 110px de largura e sobram ~32px até o grupo `.xb-actions` (no ≤389 as ações encolhem junto). Ou seja, em 320 a folga é curta: subir a logo mais que ~22px exige recompactar `.xb-actions`/hamburger. Versões: pornozinho.css 1.0.22 (header.tpl:99).
- Card do shelf de Shorts = mesmo idioma do card padrão (14/09): o item do shelf virou `div.xb-short-shelf-item > a > div.thumb-overlay.xb-portrait` (capa ÚNICA em `cover`) + `span.xb-thumb-meta` com o texto fixo **"Shorts"** (a QUANTIDADE de views foi removida a pedido — não há mais `{insert name=views}` no shelf) + `div.duration`; NADA abaixo do thumb (título e tags também removidos — o `.content-info` sumiu, então o chip "Shorts" é o único rótulo do card). Como reusa os componentes compartilhados (`.thumb-overlay/.xb-thumb-meta/.duration`), radius, borda, chip e duração vêm do tema md3 e funcionam no claro/escuro sem CSS novo. CSS: `.xb-short-shelf-item{flex:0 0 175px}` (≤767px 142px) + `.xb-short-shelf-item .thumb-overlay{aspect-ratio:9/16}` + `.thumb-overlay img{height:100%;aspect-ratio:auto;object-fit:cover}`. ARMADILHA: NÃO reusar `video_card.tpl` aqui — ele escolhe o trio 16:9 para portrait, que num quadro vertical vira 3 slots esticados; por isso o markup é próprio com uma capa. Selo "Shorts" removido; `.xb-shorts-badge-icon` passou a `var(--md-sys-color-primary)`. Foram removidas ~20 regras `.xb-short-home-*` (card/thumb/badge/duration/overlay/meta/title). Medido no headless: 175x311 desktop e 142x252 em 375 (9:16 exato), chip pill 9999px, chip bg rgba(18,15,19,.72), duration bottom/right 8px, sem overflowX; content-title 61→54 e xb-tags 57→50 (exatamente os 7 itens do shelf — os cards do grid padrão não foram tocados). Versões: pornozinho.css 1.0.25 (header.tpl:99), md3 1.0.20 (header.tpl:107 + enter.tpl:33).
- Tags dos cards em UMA linha rolável (14/09): o `.xb-tags` do `video_card.tpl` ganhou wrapper `div.xb-tags-rail` + `button.xb-tags-next` (a seta). ARMADILHA: `.xb-tags` NÃO pode virar nowrap globalmente — o cloud "Tags populares" (index.tpl:226) usa `class="xb-tags xb-tags-lg"` e precisa continuar quebrando linha; por isso tudo foi escopado em `.xb-tags-rail`. Faixa: `flex-wrap:nowrap; overflow-x:auto; scrollbar escondida; padding-right:18px`. Seta: 22px, `position:absolute` no rail (`position:relative`), vidro (`--xb-glass-bg/--xb-glass-blur`), `opacity:.5` (1 no hover), `display:none` até o JS marcar `.is-scrollable` no rail (overflow real > 2px) — então seta não aparece em card com poucas tags. Comportamento em `js/xb-tags-rail.js?ver=1.0.0` (linkado no `footer.tpl` logo após `md3-ripple.js`): tudo por delegação de evento; arraste com ponteiro só no mouse (no toque o scroll nativo já resolve) e o clique que fecha o arraste é engolido NA MESMA task (senão a tag abriria a busca); roda do mouse vira scroll horizontal (`preventDefault`); a seta avança `clientWidth*0.7` e volta ao início quando já está no fim; remarca em load/resize/`document.fonts.ready` (a largura das tags depende das fontes). Medido no headless (home, 1440): 48 faixas, 32 com overflow e exatamente 32 com seta visível, 0 setas em faixa sem overflow; todas `nowrap` e de uma linha; arraste 0→44 (44 é o scroll máximo: 343-299); clique pós-arraste não abre a tag; roda rola com `defaultPrevented`; segundo clique da seta volta a 0; sem overflowX na página; o cloud segue `flex-wrap: wrap` e fora do rail. Versões: pornozinho.css 1.0.26 (header.tpl:99).
- Ordem e capa dos shorts (14/09): (1) ORDEM — a aba padrão (`foryou`) e a vitrine da home passaram a usar um score de recência+audiência (estilo Hacker News): `ORDER BY (v.viewnumber / POW(TIMESTAMPDIFF(HOUR, FROM_UNIXTIME(CAST(v.addtime AS UNSIGNED)), NOW()) + 2, 1.5)) DESC, v.addtime DESC, v.VID DESC`. Detalhe do schema: `video.addtime` é **varchar(20) com timestamp unix**, não DATETIME — por isso o `FROM_UNIXTIME(CAST(... AS UNSIGNED))`; sem isso a idade sai errada. Substituiu `(v.orientation='portrait') DESC, (v.rate*10 + v.likes*2 + v.viewnumber) DESC` nos TRÊS lugares: `shorts.php` (só a aba foryou), `include/ajax/shorts_feed.php` (só foryou) e `index.php` ($sql_shorts). As abas Em Alta (views) e Recentes (addtime) continuam como estavam. (2) SÓ VERTICAIS — o filtro virou `$shorts_cond = " AND v.duration > 0 AND v.duration < 60 AND v.orientation = 'portrait'"` (era `$duration_cond`, sem orientação, com o retrato só priorizado no ORDER BY). O enum `video.orientation` já está populado (13/09: 7 de 7 shorts são portrait), então o filtro não tira nada hoje, mas passa a excluir shorts horizontais. (3) CAPA PELAS ESCOLHIDAS DO ADMIN — o feed lia `max(1, intval($row['thumb']))` (capa fixa) e ignorava `thumbnails_opt`; agora chama `video_rotate_cover($row)` (include/function_global.php), que sorteia um frame da lista do admin de forma estável por request — a home já fazia isso via `video_apply_cover_rotation($shorts_videos)`. `shorts_feed.php` não carregava function_global.php: adicionado `require_once` (o arquivo tem guard `AVS_FUNCTION_GLOBAL_LOADED`, então é seguro). Verificado com curl: /shorts, /ajax/shorts_feed e a home devolvem a MESMA ordem (211,137,193,148,141,142,131) e todas as capas dos shorts saem de dentro do `thumbnails_opt` de cada vídeo (211→2/16, 137→8/20, 193→9), variando entre requests. Nota: os dados de 14/09 são mínimos (7 shorts, todos do mesmo dia, todos com `thumbs=20` e `opt` preenchido) — o score com 2 no denominador favorece o mais recente; se o acervo crescer e o resultado parecer "novo demais", o ajuste fino é o `+ 2` e o expoente `1.5`.

### 14/09 — shorts: logo, player sem corte e vidro macOS
- VÍDEO NÃO CABIA NO PLAYER: o wrapper era 9:16 fixo e o vídeo `object-fit: cover` → cortava. Medido no banco, os 7 shorts têm aspectos BEM diferentes (0.5583 a **0.8083**); no VID 193 o `cover` cortava ~44% da largura. Fix: (1) helper `video_aspect_ratio($row)` em `include/function_video.php` (prefere width/height HD, cai para SD — o 211 e o 193 têm HD zerado; devolve 0 quando não há medida); (2) `shorts.php` (2 arrays) e `ajax/shorts_feed.php` expõem `'aspect'`; (3) `shorts.tpl` e `appendVideos` escrevem `style="--avs-video-ar: X"` no `.avs-player-wrapper`; (4) `avs-shorts.css`: wrapper `width: min(100%, calc(100dvh * var(--avs-video-ar, 0.5625)))` + `aspect-ratio: var(--avs-video-ar, 9/16)` + `max-height: 100%` (desktop `min(440px, ...)` / `max-height: min(100dvh,880px)`) e `.avs-video-el { object-fit: contain }` (saiu o `cover`); (5) `avs-shorts.js` refina no `loadedmetadata` (fallback p/ vídeo sem medida no banco). Verificado: boxAspect == videoAspect em TODOS os cards (ex. 211: 0.5611/0.5611; 193: 0.8056/0.8056) e o server já manda o aspecto ANTES do metadata (148/141/142 com `videoAspect: null` mas box já correto — sem salto de layout).
- LOGO NO /shorts (topo esquerdo): `<a class="xb-logo avs-shorts-logo">` em `shorts.tpl` reusa a máscara animada do header (`--xb-logo-img` inline + classe `.xb-logo` de pornozinho.css). O botão de voltar foi MANTIDO. ARMADILHA: com `.avs-shorts-tabs{position:absolute}` no desktop o topbar fica com 3 filhos e o `space-between` jogava a LOGO PARA O CENTRO (mediu 577px) — corrigido com `justify-content:flex-start` + `.avs-shorts-top-actions{margin-left:auto}` no breakpoint ≥768. Posições medidas: desktop logo em x=76 (130x26), mobile x=54 (85x17).
- TOPBAR MOBILE EM DUAS LINHAS: medido com `Emulation.setDeviceMetricsOverride` (device-width): em 390px o topbar de uma linha JÁ estava 100% saturado (38 voltar + 270 abas + 42 som = 350 de 350 úteis) → qualquer logo estourava. Então ≤767.98 ele quebra: voltar + logo + som em cima, abas centralizadas na 2ª linha (com `overflow-x:auto` para telas muito estreitas). Altura final 108px; sem overflow em 390 e 320. Abas compactadas (12.5px) e botões 36px no mobile; `≤400` a logo cai para 17px.
- VIDRO MACOS ("vidro embaçado") em setas e chips: tudo usa os tokens `--xb-glass-bg-dark/--xb-glass-border/--xb-glass-hl/--xb-glass-blur(-sm)` (que são REPUBLICADOS com valores próprios no :root do md3 — os valores resolvidos na página são `rgba(18,15,19,.72)`, `rgba(255,255,255,.16)`, `blur(24px) saturate(1.8)` / `blur(12px) saturate(1.6)`). Alvo: `.xb-shelf-btn` (setas do shelf, 34px radius 11px), `.xb-carousel-arrow` (base em pornozinho.css + bloco `.xb-carousel-sec--themed` DENTRO de video_carousel.tpl, que era rosa), `.xb-tags-next` (seta do trilho de tags, radius 8px), `.duration` (tempo/qualidade), `.hd-text-icon` (saiu o gradiente rosa → selo branco translúcido) e `.xb-thumb-meta` (título/views; regra do md3 é a que vence). O hover clareia (`rgba(255,255,255,.18)`), NÃO pinta de rosa. Regra de ouro mantida: blur só aparece com pixels atrás.
- Versões: pornozinho.css **1.0.27** (header.tpl:99), md3 **1.0.21** (header.tpl:107 + enter.tpl:33), avs-shorts.css **1.0.3** (header.tpl:110), avs-shorts.js **1.0.1** (footer.tpl:550). Verificação: lint nos 4 PHP + browser headless (aspecto/logo/topbar/estilos computados). Gap: aparência não conferida a olho; `video_carousel.tpl` só foi validado por CSS lido, não medido na página (carrossel temático só aparece com categoria aleatória na home).
- ABERTO — deep link `shorts?v=<vid>`: o usuário relatou "não respeitou o vídeo". Reproduzido no Chrome headless via CDP e NÃO consegui reproduzir a falha: `/shorts?v=148` faz o server PREPENDER 148 (`shorts.php` monta `$initial_videos` com o `initial_vid` primeiro e depois o feed) e o card 148 toca; o mesmo vale em navegação na MESMA aba (`?v=131` → `?v=211` → `?v=142`) e em reload, sempre com `scrollTop=0` e `history.scrollRestoration='auto'` sem restaurar posição. Ou seja, o deep link depende de o server colocar o vídeo em 1º (há uma amarração implícita) — se o vídeo não passa nos filtros (`duration>0 AND duration<60`, `$active_cond`, `get_video_sources()` vazio) o `shorts.php` cai no feed PADRÃO silenciosamente, sem aviso nem redirect. Detalhe conhecido: `avs-shorts.js:120` faz `replaceState` para `/shorts?v=<vid>` e DESCARTA o `?tab=` (link copiado de "Em Alta" reabre em "Para Você"). O JS também NUNCA lê `data-initial-vid`. Falta do usuário: em que fluxo/dispositivo viu a falha (aba do shelf da home? link compartilhado? mobile?).

### 14/09 — shorts: ajustes de UI, Destaques fora e TEMA CLARO
- SETA DAS TAGS NÃO FLUTUA MAIS SOBRE ELAS: `.xb-tags-rail` virou `display:flex; align-items:center; gap:6px` e o `.xb-tags` é `flex:1 1 auto; min-width:0` — a seta (`.xb-tags-next`) é item do flex DEPOIS da faixa, então empurra em vez de cobrir (antes era `position:absolute; right:-3px`, com `padding-right:18px` de espaçador na faixa; ambos saíram). O `margin-top:7px` migrou de `.xb-tags` para o rail para o centro da seta cair na linha das tags. Sem laço de realimentação: como a seta só entra com overflow já existente e fica `display:none` quando não há o que rolar, aparecer só AUMENTA o overflow. Medido (home 1440 e 390): 0 sobreposições com qualquer tag visível (recortando o rect da tag pelo rect da faixa — sem isso as tags fora de vista dão falso positivo), seta 100% fora da faixa, centro alinhado, sem overflowX. Versão: pornozinho.css 1.0.28.
- /shorts — MOLDURA (pedidos do usuário): (1) logo com `margin-left:12px` (no mobile o topbar NÃO tem gap, então 4px colava a logo no botão de voltar; medido: gap desktop 14→22px, mobile 4→12px); (2) badge "Toque para ativar o som" desceu de `top:80px` para **120px** (132px no ≤767.98) — ARMADILHA: ele é posicionado dentro do `.avs-player-wrapper` (que no desktop começa em y=58), então `top` é relativo ao player: 80px dava y=138 no desktop mas y=79 no MOBILE, ou seja EM CIMA das abas (o topbar móvel tem 108px); agora y=178 desktop / y=119 mobile (11px abaixo do topbar); (3) `.avs-short-title` #f5f5f5→**#fff**; (4) DISCO DE VINIL GIRATÓRIO REMOVIDO (`.avs-music-disc` + `.disc-art`) do `shorts.tpl` E do template do `appendVideos` em `avs-shorts.js` — se esquecer o do JS, os cards do infinite scroll voltam com o disco. `@keyframes avsSpin` NÃO pode sair: o `.avs-spinner` usa a mesma.
- AUTOR ACIMA DO TÍTULO REMOVIDO: o bloco `.avs-short-creator` (avatar + @username + botão Seguir) saiu do `.avs-short-meta-bottom` no `shorts.tpl` e no template JS; o criador segue na barra lateral `.avs-action-profile` (avatar + botão `+`), e o `triggerSubscribe` já atualizava os dois — o loop `.avs-btn-follow` apenas não casa mais nada. CSS morto removido (`.avs-short-creator/.avs-short-avatar/.avs-short-username/.avs-btn-follow*`). Verificado: `primeiroFilhoMeta = avs-short-title-wrap` tanto no card do servidor quanto no injetado pelo AJAX; meta desceu 640→688 mas o título ficou no mesmo y (708).
- DESTAQUES FORA DA HOME: a seção `{if $hero_videos}` + o `<script>` do autoplay do `#xb-hero-video` saíram do `index.tpl` (237→161 linhas). Ordem agora: Shorts & Reels → Novos vídeos → Em alta → carrossel → Creators. **PENDÊNCIA CONSCIENTE**: `index.php` ainda roda a query do hero (`$hero_videos`, ~35 linhas incluindo o loop de `get_video_sources()` que resolve URL assinada do mini-clip) e o `$smarty->assign('hero_videos', ...)` sem consumidor nenhum; o CSS `.xb-hero-*` de pornozinho.css (63 ocorrências) ficou **dormente**. Não removi para manter o diff mínimo — limpar quando o usuário autorizar.
- TEMA CLARO NO /SHORTS (pedido do usuário): bloco novo no FIM do `avs-shorts.css` com `html[data-theme="light"]` (o tema é setado pelo script inline do header.tpl e o /shorts inclui header.tpl, então funciona). DECISÃO registrada no próprio CSS: quem tem SUPERFÍCIE própria clareia (fundo da página/tela/card, pílulas do topbar e do som, abas, botões de ação, nav lateral, painel de comentários inteiro + rodapé/form/input, toast, loader, estado vazio, ambient blur que em vez de escurecer clareia) e quem é desenhado SOBRE pixels do vídeo segue BRANCO com o scrim escuro (título, descrição, faixa de áudio, contadores/`.avs-action-label`, scrim do meta) — texto escuro sobre frame arbitrário é ilegível. O box do player segue preto nos dois temas. ARMADILHA DO TEMA CLARO: o bloco light do md3 força `h1..h6 { color: var(--xb-text) }` (escuro) e `a { color: #5A4A4F }` com especificidade (0,1,2)/(0,1,1), que VENCEM regras de classe da página — foi o que deixou o `.avs-short-title` (um `<h2>`) escuro e o `.avs-btn-login-prompt` (um `<a>`) cinza sobre o gradiente; os dois precisaram de override explícito `html[data-theme="light"] .avs-...`. Vale a mesma checagem para qualquer `<h*>`/`<a>` novo desenhado sobre mídia. `.avs-shorts-tabs`, `.avs-shorts-back` e `.avs-shorts-tab` também são `<a>`: já cobertos. Medido no headless (dark ANTES vs light): fundo página/app/card `#FFF8F8`, pílulas `rgba(255,255,255,.72)`, abas `.78`, ação `.78`, drawer `.86`, toast `.95`, texto do painel `rgb(30,21,25)`, título/música/labels brancos, scrim e player intactos; o snapshot DARK ficou IDÊNTICO ao de antes do bloco (sem regressão). Versão: avs-shorts.css 1.0.8, avs-shorts.js 1.0.3.
- Coleta de prova útil: com `Emulation.setDeviceMetricsOverride` + perfil novo (`--user-data-dir`), `localStorage.setItem('avs_theme','light')` + reload é o jeito barato de testar o tema claro; markup que só existe logado (form de comentário) pode ser INJETADO no DOM para medir o CSS computado.

### 14/09 — PRODUÇÃO: "/shorts sem estilo" = OPcache velho no deploy (não era o CSS)
- SINTOMA: em `https://pornozinho.com/shorts` a página renderiza SEM estilo (app `position:static`, viewport 1280 no celular, cards empilhados) e o feed infinito 500. LOCALMENTE tudo certo — o problema é do DEPLOY/VM.
- DIAGNÓSTICO (por evidência, sem acesso ao shell da VM): (1) os `.tpl` NO SERVIDOR estão CORRETOS — `curl https://pornozinho.com/templates/frontend/pornozinho/header.tpl` traz `pornozinho.css?ver=1.0.28`, `avs-shorts.css?ver=1.0.8` e os `xb-nav-icon`; (2) mas o HTML SERVIDO referencia `pornozinho.css?ver=1.0.18`/`md3 1.0.16`, NÃO tem o `<link>` do `avs-shorts.css` e nem `xb-nav-icon` → o header sai de uma compilação ANTIGA; (3) outros templates saem FRESCOS no mesmo HTML (`xb-tags-rail`/`xb-tags-next` do `video_card.tpl`, `xb-short-shelf-item` do `index.tpl`) → é POR ARQUIVO; (4) a listagem pública de `/cache/frontend/` mostra que o `*header.tpl.php` FOI reescrito no mesmo minuto do deploy (18:26) — ou seja, o Smarty recompilou, mas o PHP executa o **bytecode antigo do OPcache**: `systemctl reload apache2` NÃO limpa OPcache em mod_php e o OPcache da VM não está revalidando por mtime (`validate_timestamps=0`). O mesmo mecanismo explica os **500 em TODOS os endpoints `/ajax/*`** (inclusive módulos que eu não toquei: `vote_video`, `search`, `related_videos`, `user_pagination` — todos 500 com corpo VAZIO = fatal com `display_errors=off`) — é a cadeia de boot do `ajax.php` rodando código pré-fix (DB/.env). LIÇÃO: sintoma "template novo no disco, HTML velho" = cache de compilação/OPcache, não CSS.
- FIX aplicado em `.github/workflows/deploy-vm.yml` (é o deploy que precisa mudar): rsync passou a EXCLUIR `cache`, `tmp` e `logs` (antes um `cache/frontend` velho podia viajar junto) e ganhou o step **"Invalidate template & opcode caches"** antes do reload: `find templates -name '*.tpl' -exec touch {} +` (o Smarty compara MTIME do .tpl com o do PHP compilado: `.tpl` com mtime mais antigo que o cache NUNCA é recompilado), `rm -rf cache/frontend cache/backend` + recriação com dono `www-data`, e **`systemctl restart apache2`** (+ `restart 'php*-fpm'` se existir) porque só o restart limpa o OPcache. Para consertar sem esperar deploy, rodar isso na VM: `cd /var/www/avscms && sudo find templates -name '*.tpl' -exec touch {} + && sudo rm -rf cache/frontend cache/backend && sudo mkdir -p cache/frontend cache/backend && sudo chown -R www-data:www-data cache && sudo systemctl restart apache2`.
- ACHADOS DE SEGURANÇA (vhost `deploy/avscms-prod.conf` bloqueia só `include|deploy|sql`): `/cache/frontend/`, `/tmp/` e `/tmp/logs/` estão com Directory Listing PÚBLICO (dá para listar os templates compilados e baixar os `*.grabber.log`). Bloquear também `cache`, `tmp` e `logs` no `DirectoryMatch`. Lembrar que o vhost é instalado À MÃO na VM (instalar + `systemctl reload apache2`).
- RISCOS DO DEPLOY (revisar com o usuário): `rsync --delete` apaga na VM tudo que não está no checkout — e `include/config.local.php`, `include/config.gcs.php` e `include/gcs-service-account.json` são GITIGNORED (não estão no repo). O site funciona, então na prática eles sobrevivem lá (ou são restaurados por algo fora do workflow do repo) — mas vale confirmar, porque um deles faltando derruba `ajax.php` (que faz `require '/language/'.$_SESSION['language'].'.lang.php'`) e/ou o GCS. Como diagnosticar produção sem shell na VM: `curl` no `.tpl`/`.css` (dão o estado do disco) + Chrome headless via CDP na URL pública (dão o HTML/estilos/computed) + `/cache/frontend/` e `/tmp/logs/` (listagens públicas).
